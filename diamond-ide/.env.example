# IDE environment variables (optional)
# Copy to .env.local and adjust values as needed.

# Backend API URL for server-side compilation fallback.
# Leave unset to use WASM-only mode (recommended for public deployments).
# NEXT_PUBLIC_API_URL=http://localhost:4000
